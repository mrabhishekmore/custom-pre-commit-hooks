- id: check-sonar-quality-gate
  name: Check SonarQube Quality Gate
  entry: scripts/sonar_checks.py
  language: python # Add more if your script needs it
  additional_dependencies: [requests]
  pass_filenames: false
  verbose: true
